apiVersion: v1
kind: Service
metadata:
  name: {{ include "ipxe-api.fullname" . }}
  labels:
    {{- include "ipxe-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ipxe-api.selectorLabels" . | nindent 4 }}
